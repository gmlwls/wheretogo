로그인한 유저 : <%=@user_id%><br><br>
<%=link_to '모임 생성하기!',create_path,method: :get%><br>
룸 리스트<br>
<% @rooms.each do |room| %>
  <%=link_to show_path(room.id) do%>
  <%=room.id%>번 방 / 만든 사람 id : <%=room.creator%><% end %> <%=link_to '참여!', join_path(room.id) %><br>
<% end %>